.venv\Scripts\activate

后端：
 python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

前端：
cd frontend; npm run dev
 

背景：
我这个项目需要使用ai大模型，我在这个项目中尝试使用copilot的ai大模型。因为我的copilot是公司账号，可以使用gpt5、claude-sonnet-4.5等优秀大模型。 
目前我已经使用models.inference.ai.azure的方式，可以使用gpt5了，但是无法使用claude-sonnet-4.5。
我需要你做的事情如下：
1、目前通过models.inference.ai.azure的方式的代码你保留，不要删掉，作为备份方案； 
2、去github搜索相关的解决方案：因为我目前使用idea的copilot插件是完全可以使用gpt5、claude-sonnet-4.5等优秀大模型的，你尝试去模拟idea的copilot插件的方式去接人大模型。 3、代码在tradingagents\llm_adapters\copilot_adapter.py中
注意：
1、你执行python脚本时需要先使用.venv\Scripts\activate激活虚拟环境；
2、powershell中执行多行命令不要使用&，应该使用分号。